// Test: encode() returns bytes, decode() accepts bytes, roundtrip works
#[wire]
struct Point {
    x: i32,
    y: i32,
}

fn main() {
    let p = Point { x: 42, y: 99 };
    let buf: bytes = p.encode();
    let p2 = Point.decode(buf);
    println(p2.to_json());
}
